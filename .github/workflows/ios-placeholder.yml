# Deprecated: use ios.yml + docs/FASTLANE_MATCH.md
# Kept as a no-op redirect so old bookmarks do not 404 in the Actions UI history.
name: iOS (moved)

on:
  workflow_dispatch:

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - name: Use the real workflow
        run: |
          echo "This placeholder is retired."
          echo "Use Actions → 'iOS TestFlight' (.github/workflows/ios.yml)"
          echo "Docs: docs/FASTLANE_MATCH.md and docs/IOS_SIGNING.md"
