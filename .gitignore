node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env
.env.local
.env.*.local
!.env.example
# Capacitor native projects ARE committed (needed for CI + store builds).
# Only signing secrets + synced web assets are excluded (run: npm run cap:sync).
.idea/
android/app/src/main/assets/public/
ios/App/App/public/
.vscode/
*.swp

# Signing / certificates — never commit
*.jks
*.keystore
key.properties
**/key.properties
orange-groove-release.jks
orange-groove-upload.jks
upload_certificate.pem
*.mobileprovision
AuthKey_*.p8
*.p12
